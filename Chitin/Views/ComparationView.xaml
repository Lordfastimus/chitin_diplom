<UserControl x:Class="Chitin.Views.ComparationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chitin.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0"  Text="Имя программы:"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding AnalyzeInfo_1.ProgrammName}"/>
            <TextBlock Grid.Row="0" Grid.Column="0"  Text="Дата анализа:"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding AnalyzeInfo_1.AnalyseDate}"/>
        </Grid>
        
        
    </StackPanel>
</UserControl>
